import type { FastifyInstance } from 'fastify';

export async function backlinksRoutes(_fastify: FastifyInstance) {
  // Backlinks are managed via project routes (/projects/:id/backlinks)
  // This file is for any standalone backlink operations if needed
}
