import type { FastifyInstance } from 'fastify';

export async function messagesRoutes(_fastify: FastifyInstance) {
  // Messages are managed via project routes (/projects/:id/messages)
  // This file is for any standalone message operations if needed
}
